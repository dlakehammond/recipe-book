# Recipe Book

A simple CLI tool for storing and searching recipes.

## Usage

```bash
python -m recipe_book add "Pancakes" --tag breakfast
python -m recipe_book list
python -m recipe_book search "pancakes"
python -m recipe_book show 1
```
